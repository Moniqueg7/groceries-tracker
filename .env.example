# --- Vercel (required at runtime) ---
# Create free Postgres at https://neon.tech → copy pooled connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"

# --- Local dev (pick one) ---
# Option A: same Neon URL as production
# Option B: Docker — run: docker compose up -d
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/groceries?schema=public"

# Optional: live web prices in Search tab
# SERPAPI_API_KEY=""
